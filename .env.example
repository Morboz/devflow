# Copy to .env and fill in real values. .env is gitignored.

# --- Database (ADR-0013: PostgreSQL) ---
DATABASE_URL=postgres://devflow:devflow@localhost:5433/devflow
DATABASE_URL_TEST=postgres://devflow:devflow@localhost:5433/devflow_test
# Admin connection (to the maintenance DB) used to create the test DB.
DATABASE_URL_ADMIN=postgres://devflow:devflow@localhost:5433/postgres

# --- GitHub App (ADR-0008: minimal permissions) ---
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY=        # path to the .pem, or the key contents
GITHUB_INSTALLATION_ID=
GITHUB_WEBHOOK_SECRET=     # used to verify X-Hub-Signature-256 (D2)

# --- Execution Engine provider config (ADR-0009; used by D8) ---
DEVFLOW_PROVIDER_MODEL=
DEVFLOW_PROVIDER_API_KEY=
